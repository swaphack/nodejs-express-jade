// 动作
var actions = (function (mod) {
    var fps = 1000 / 30;
    // 执行一次
    mod.moveTo = function (pos, delta) {
    };

    // 重复执行
    mod.moveBy = function (func, interval) {
    };

    return mod;
}({}));